{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the lecture assistant window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
