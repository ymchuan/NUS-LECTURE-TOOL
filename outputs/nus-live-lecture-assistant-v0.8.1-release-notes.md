# NUS 课堂同传 v0.8.1

## 结构化总结解析修复

- 修复阿里云模型偶尔在 JSON 字符串中返回裸换行、Tab 或其他控制字符时，阶段总结和整课总结解析失败的问题。
- 解析器仍会优先执行严格 JSON 解析，仅在检测到字符串内部控制字符时进行安全转义并重试。
- 无法修复的结构错误仍会明确报错，不会保存不完整的总结。

## 验证

- Rust 总结解析测试通过。
- 前端单元测试、TypeScript 构建和 macOS 桌面应用构建通过。
