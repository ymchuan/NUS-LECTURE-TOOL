# NUS 课堂同传 v0.13.0

## v0.13.0 更新

- 实时转写推荐 Deepgram Nova-3。
- OpenAI 文本接口支持实时翻译、阶段总结、整课总结分别配置 API Key 和模型。
- 默认成本策略调整为 `gpt-5.6-luna` 翻译、Luna/Terra 阶段总结、`gpt-5.6-terra` 整课总结。
- 结束课堂时可选择是否生成整课总结，跳过时不产生整课总结模型费用。
- 旧版单一 OpenAI Key 与旧模型配置保持兼容回退。
- Ollama 限定为逐条语段的应急补翻来源；全局实时翻译、阶段总结、整课总结和 AI 问答继续使用 OpenAI/阿里云。
- 语段翻译图标打开独立补翻弹窗，可选当前云端配置、Ollama 本地或 Ollama 云端；模型和地址在此保存，不改全局模型。Ollama 云 Key 使用独立系统凭据库槽，固定请求官方云地址。
- Ollama 本地请求关闭思考并保温 5 分钟，使用固定上下文/输出上限；超出输入预算明确报错，不静默丢文本，也不自动切付费服务。
- 补翻支持当前课堂与历史课堂；迟到响应通过独立请求 ID 隔离，补翻失败保留原译文。
- 翻译流式响应必须收到完整结束事件；连接提前结束、Ollama 长度截断和本地错误会显示为可重试状态。

## 本次修复

- 限制 Vite 文件监听与依赖入口扫描范围，避免在 exFAT 上遍历 Rust 编译缓存导致开发服务请求超时、桌面窗口白屏；不改变模型配置与课堂数据。
- 阶段总结新增后，不再无条件把左侧阅读位置拉到最底部。
- 只有用户原本位于总结列表底部时，才会继续自动跟随最新总结。
- 用户上滑回顾旧总结后，保持当前阅读位置，并显示“回到最新”按钮。
- 有新总结产生时，按钮会显示尚未查看的总结数量。
- 点击“回到最新”或手动滑到底部后，会恢复自动跟随。
- 切换课程、开始新课堂、打开历史课堂和返回当前课堂时，滚动状态会正确重置。

## 说明

- 此行为与右侧实时记录一致，回顾旧内容时不会再被新内容打断。
- 当前正在运行的旧版应用不会被关闭或替换；安装并重新启动 v0.13.0 后生效。

## 本轮检查（2026-09-07）

- 前端测试 57 项通过；Rust 测试 67 项通过，1 项需要额外课堂数据的回放测试未运行。
- TypeScript/Vite 生产构建、Rust 格式检查通过。
- 桌面与 390px 窄屏检查通过；模拟 IPC 验证了当前课堂重译、失败保留旧译文、本地补翻和历史补翻的保存参数。未调用付费云端 API。
- 本机已下载并测试 `qwen2.5:3b`：RTX 5050 8 GB、100% GPU，预热后不同课堂句子的接口完整输出约 0.33-0.54 秒，不包含 ASR/断句。7B 未下载，Ollama 云端未使用真实 Key 发起付费请求，尚未重新生成安装包。

## 原始交付记录（历史）

- 前端单元测试：44 项通过。
- Rust 单元与集成测试：52 项通过，1 项真实课堂回放诊断测试按需运行。
- TypeScript 生产构建、Rust 格式检查、Clippy 严格检查和 macOS Tauri 正式打包：通过。
- 实际滚动测试：上滑后新增总结时位置保持不变；新增数量提示和恢复跟随均正常。
- 阶段总结阈值、翻译请求超时/迟到响应隔离、Ollama 请求体/NDJSON 完成标志和历史语段单行保存均有单元测试覆盖。
- macOS 压缩包解压后通过严格签名与版本校验，源码和应用产物未发现 API Key。
